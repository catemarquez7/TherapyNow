<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:padding="16dp">

    <LinearLayout
        android:id="@+id/headerCrear"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="24dp">

        <ImageButton
            android:id="@+id/btnVolverCrear"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_media_previous"
            app:tint="@color/primary"
            android:contentDescription="Volver" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="Nueva Nota"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="@color/on_background" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/headerCrear"
        android:layout_above="@+id/btnGuardarNota"
        android:orientation="vertical"
        android:layout_marginBottom="16dp">

        <EditText
            android:id="@+id/etTituloNota"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Título de la nota..."
            android:inputType="textCapSentences"
            android:textColor="@color/on_background"
            android:textColorHint="#999999"
            android:textSize="18sp"
            android:textStyle="bold"
            android:background="?android:attr/editTextBackground"
            android:padding="12dp"
            android:layout_marginBottom="16dp"/>

        <EditText
            android:id="@+id/etContenidoNota"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:hint="Escribí acá los detalles e ideas de la sesión..."
            android:inputType="textMultiLine"
            android:gravity="top"
            android:textColor="@color/on_background"
            android:textColorHint="#999999"
            android:textSize="16sp"
            android:background="?android:attr/editTextBackground"
            android:padding="12dp"/>
    </LinearLayout>

    <Button
        android:id="@+id/btnGuardarNota"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="Guardar Nota"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:backgroundTint="@color/primary"
        android:padding="14dp"/>

</RelativeLayout>